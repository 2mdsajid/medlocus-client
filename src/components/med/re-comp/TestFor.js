import React from 'react'

function TestFor({onClickEvent}) {
  return (
    <div>
      <button id='testbtn id' onClick={onClickEvent}>click me to trigger</button>
    </div>
  )
}

export default TestFor
